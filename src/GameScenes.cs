using System;

/// <summary>
/// An enum corresponding to the different Unity scenes in KSP
/// </summary>
public enum GameScenes
{
    LOADING = 0,
    LOADINGBUFFER = 1,
    MAINMENU = 2,
    SETTINGS = 3,
    CREDITS = 4,
    SPACECENTER = 5,
    EDITOR = 6,
    FLIGHT = 7,
    TRACKSTATION = 8,
    SPH = 9,
    PSYSTEM = 10,
}
